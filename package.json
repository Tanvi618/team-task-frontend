{
  "name": "client",
  "version": "0.0.0",
  "private": true,

  "engines": {
    "node": "24.x"
  },

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}